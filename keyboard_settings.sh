#!/bin/dash

# delay before starting to reapeat the letter when holding a key
gsettings set org.gnome.desktop.peripherals.keyboard delay 250

# delay between each repetition of the letter when holding the key
gsettings set org.gnome.desktop.peripherals.keyboard repeat-interval 30
